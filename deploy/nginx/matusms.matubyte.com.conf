# MatuSMS Dashboard — matusms.matubyte.com
# Copiar a: /etc/nginx/sites-available/matusms.matubyte.com
# Enlazar:  sudo ln -sf /etc/nginx/sites-available/matusms.matubyte.com /etc/nginx/sites-enabled/

# Ruta del build de Vite (ajustar si clonas en otra carpeta)
root /var/www/matusms/apps/web/dist;
index index.html;

server {
    listen 80;
    listen [::]:80;
    server_name matusms.matubyte.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name matusms.matubyte.com;

    root /var/www/matusms/apps/web/dist;
    index index.html;

    ssl_certificate     /etc/letsencrypt/live/matusms.matubyte.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/matusms.matubyte.com/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    access_log /var/log/nginx/matusms-web.access.log;
    error_log  /var/log/nginx/matusms-web.error.log;

    # Cache de assets con hash (Vite)
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # SPA — TanStack Router (rutas del cliente)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Seguridad básica
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
}
